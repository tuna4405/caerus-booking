// Route: /admin/events/new 🔒👑 — POST /events
export default function CreateEvent() {
  // TODO: event form (title, description, startsAt, durationMinutes,
  // auditorium, price), submit via api/client.js
  return <h1>Create Event — TODO</h1>;
}
