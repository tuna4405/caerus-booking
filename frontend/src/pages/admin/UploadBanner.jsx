// Route: /admin/events/:id/banner 🔒👑 — POST /events/:id/banner
// (multipart/form-data, field name "image")
export default function UploadBanner() {
  // TODO: file input (jpg/png, max 5MB), submit via api/client.js
  return <h1>Upload Banner — TODO</h1>;
}
