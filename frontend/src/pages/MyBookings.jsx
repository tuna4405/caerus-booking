// Route: /my-bookings 🔒 — GET /bookings, falls back to mocks/bookings.json
export default function MyBookings() {
  // TODO: fetch bookings via api/client.js, render <BookingCard> per booking
  return <h1>My Bookings — TODO</h1>;
}
