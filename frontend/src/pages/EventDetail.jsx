// Route: /events/:id — GET /events/:id + GET /events/:id/seats, then
// POST /bookings on confirm. Includes <SeatMap>.
export default function EventDetail() {
  // TODO: fetch event + seat map via api/client.js, render <SeatMap>,
  // submit selected seats via createBooking()
  return <h1>Event Detail — TODO</h1>;
}
