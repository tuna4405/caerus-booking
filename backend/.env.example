# Local Postgres (matches docker-compose.yml)
DATABASE_URL=postgresql://caerus:caerus_dev@localhost:5432/caerus

# JWT
JWT_SECRET=changeme_dev_secret

# Server
PORT=3000

# AWS (placeholders — real values come from the team's AWS setup)
AWS_REGION=ap-southeast-1
S3_BUCKET_IMAGES=caerus-images
S3_BUCKET_TICKETS=caerus-tickets
